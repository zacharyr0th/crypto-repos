// Export common components for consistent UI elements across the application
export { ErrorBoundary } from './ErrorBoundary';
export { Navbar } from './Header';
export { DashboardWrapper } from '../dashboard/DashboardWrapper';

// Re-export sidebar components
export * from '../sidebar';
